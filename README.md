# LMedText-Binary-Classification-Trainer
This tool supports binary classification modeling for labeled text. Its Python code integrates decision tree, random forest, logistic regression, SVM, LightGBM, and CatBoost. Originally developed to build binary text classifiers for medical text, it functions as a general-purpose tool for binary text classification across scenarios.
